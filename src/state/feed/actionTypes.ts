const loadFeed$ = 'feed/loadFeed$';

const SET_DATA = 'feed/SET_DATA';
const SET_LOADING = 'feed/SET_LOADING';
const SET_ERROR = 'feed/SET_ERROR';

export default {
  loadFeed$,

  SET_DATA,
  SET_LOADING,
  SET_ERROR,
};
